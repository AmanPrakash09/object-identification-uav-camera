# Get sample images for testing

Go to `phase-2\visdrone-dataset\test-dev\images\` and copy any `JPG` file into this directory.
To test that image, go to `phase-2\jetson-nano\trained_rfdetr_picture_input.py` and replace line 17 with the file name of the image.